import React from 'react';
import { Outlet } from 'react-router-dom';

const ServicesLayout = () => {
  return (
    <div>
      ServicesLayout <Outlet />
    </div>
  );
};

export default ServicesLayout;
